const FormHeader = ({ title, description, date }) => {
  return (
    <div>
      aaaaaaaaaaaaaaaaaa
      <h1>{title}</h1>
      <span>{description}</span>
      <p>{date}</p>
    </div>
  );
};

export default FormHeader;
